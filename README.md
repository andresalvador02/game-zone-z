# 🎮 Game Zone Z (Migrated & Improved)

Esta versión fue reconstruida combinando tu proyecto antiguo con la nueva estructura PHP + MariaDB.
- Mantén `config/db.sample.php` como plantilla y crea `config/db.php` con tus credenciales locales.
- Importa `database/gamezone.sql` con XAMPP Shell o phpMyAdmin.
- Carpeta `assets/` contiene CSS, JS e imágenes originales cuando existían.
